class Solution(object):
    def missingNumber(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        """
        Time complexity: O(n)
        Space complexity: O(1)
        """
        for i in range(len(nums)+1):
            if i not in nums:
                return i
